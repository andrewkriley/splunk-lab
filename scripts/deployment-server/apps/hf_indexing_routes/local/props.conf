# Heavy forwarder parsing + routing rules. UFs cannot apply TRANSFORMS — that's
# why this app targets the heavy_forwarders server class only.

[syslog]
TRANSFORMS-drop_comments = drop_comment_lines

[default]
TRANSFORMS-set_index = route_by_sourcetype
